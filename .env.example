# ─── AI Chatbot Configuration ────────────────────────────────────────────────
#
# To enable the AI chatbot with real Claude AI responses:
#
# 1. Go to https://console.anthropic.com and create a free account
# 2. Create an API key
# 3. Copy this file and rename it to .env (in the project root)
# 4. Replace the placeholder below with your actual API key
#
# Copy this file to .env:
#   cp .env.example .env
#
# ─────────────────────────────────────────────────────────────────────────────
VITE_ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# ─── Contact Form (EmailJS) ───────────────────────────────────────────────────
# The Contact section uses EmailJS. Update the values in:
#   src/sections/Contact.tsx  (search for EMAILJS_SERVICE_ID)
#
# 1. Go to https://www.emailjs.com and sign up (free)
# 2. Create an Email Service → copy Service ID
# 3. Create a Template with vars: {{from_name}} {{from_email}} {{subject}} {{message}} {{to_name}}
# 4. Copy Template ID and Public Key into Contact.tsx
# ─────────────────────────────────────────────────────────────────────────────
